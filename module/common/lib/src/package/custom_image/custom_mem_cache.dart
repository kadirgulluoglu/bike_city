/// Custom memory cache for the image
final class CustomMemCache {
  /// Constructor for the custom memory cache
  const CustomMemCache({
    this.height = 300,
    this.width = 300,
  });

  /// Height of the cached image
  final int height;

  /// Width of the cached image
  final int width;
}
